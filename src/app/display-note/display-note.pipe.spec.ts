import { DisplayNotePipe } from './display-note.pipe';

describe('DisplayNotePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayNotePipe();
    expect(pipe).toBeTruthy();
  });
});
